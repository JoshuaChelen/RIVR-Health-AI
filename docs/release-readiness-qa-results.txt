RIVR Health AI release-readiness implementation evidence

Objective source:
- docs/release-readiness-agent-plan.txt

Release-readiness commits:
- 167c450 harden timeline UX and release checks
- 0d8f499 document release readiness verification gaps
- f903abf scope app typecheck for release validation
- 7bc02b0 feature-flag timeline voice search
- c386691 dedupe expo config plugin dependency
- 599ce1a add timeline helper regression tests
- 77932a7 add expo lint gate
- 5519473 apply safe eslint autofixes

Follow-up dependency hardening:
- Expo SDK patch versions aligned to Expo Doctor expected versions.
- assets/branding/adaptive-icon.png padded to a square 264x264 canvas.
- package.json adds Expo Doctor React Native Directory exclusions for:
  - react-native-health
  - react-native-document-picker
- package.json adds a postcss override for @expo/metro-config to remove the moderate PostCSS audit finding without downgrading Expo.
- package.json overrides @expo/config-plugins to 54.0.4 so react-native-health uses the Expo SDK 54 plugin stack instead of installing stale nested Expo packages.

Requirement-to-artifact checklist:

1. Timeline stability and backward compatibility
- Evidence:
  - src/lib/timeline.ts adds normalizeTimelineEvent, normalizeStoredDate, normalizePrecision, tag/data coercion, and safe defaults for missing title/category/source/summary/included_in_previsit/document fields.
  - src/screens/App/TimelineScreen.tsx normalizes rows from Supabase before render.
  - src/screens/App/TimelineEventDetailsScreen.tsx normalizes rows from Supabase before edit/display and saves date_precision as null when occurred_at is cleared.
  - src/components/ui/Timeline/TimelineCard.tsx separates the navigation Pressable from the Pre-Visit Switch footer. This removes the nested switch-inside-navigation press target that is a likely mobile gesture conflict/freeze source.
- Status:
  - Implemented.
  - Needs authenticated device/simulator QA to prove the reported mobile freeze is fully resolved.

2. Timeline date and patient age UX
- Evidence:
  - src/lib/timeline.ts adds ageAtIncident, formatTimelineDateMain, and formatTimelineDateDetail.
  - Timeline main view shows incident year by default for older/coarser events.
  - Timeline main view shows detailed date only for day-precision events within the last 24 months.
  - Details screen shows full incident date detail and a sentence including age when DOB is available.
  - Details screen separates Incident date and Reported date rows.
  - Missing incident/reported dates fall back to explicit unknown text.
- Status:
  - Implemented.
  - Needs manual QA with seeded old/new records for exact display confirmation.

3. Clinical highlighting and formatting
- Evidence:
  - src/lib/timeline.ts adds clinicalTagsForEvent and normalizeClinicalLabel.
  - Timeline cards render clinical scan chips for side, body part, diagnosis, surgery, procedure, injury, medication, and symptom signals when detected.
  - Details screen renders the same clinical chips and normalizes clinical data labels from legacy data keys.
- Status:
  - Implemented as deterministic, low-risk local formatting.
  - Detection is intentionally conservative; it does not use AI or broad NLP.

4. Multimodal search UX
- Evidence:
  - src/lib/timeline.ts adds timelineMatchesQuery and healthCardMatchesQuery.
  - src/screens/App/TimelineScreen.tsx adds typed natural search across timeline event text, dates, tags, data payload, document title, clinical tags, and health card / AI summary JSON.
  - Search result count and no-result state are implemented.
  - Health card matches show a small result card linking to Health Summary.
  - Voice search has a release-safe UI entry point behind EXPO_PUBLIC_ENABLE_VOICE_SEARCH=true.
  - When voice search is disabled or unavailable, the app shows a typed-search fallback instead of adding native speech permissions/dependencies.
  - The enabled path uses platform speech support on web only; native mobile keeps the typed-search fallback for App Store safety.
- Status:
  - Typed search implemented.
  - Voice search is feature-flagged with a clear fallback.
  - Automated tests now cover representative natural prompts for timeline and health-card matching.

5. App-wide release quality audit
- Evidence:
  - Static/code checks run below.
  - Web dev server compiled and served the Expo web shell.
- Status:
  - Incomplete. Full authenticated manual QA across login, signup, documents, upload, share package, QR/copy/share modal, and timeline edit/save flows requires a simulator/device and test credentials or seeded data.

6. Testing and validation
- Commands run and results:
  - npm install
    - Passed after dependency updates.
  - npm run lint
    - Failed: package.json has no lint script.
    - Re-run at final verification: still missing script.
    - Added Expo ESLint config and re-run after escaping JSX text errors: passed with 0 errors and 25 warnings.
    - Re-run after eslint --fix cleanup: passed with 0 errors and 16 warnings.
  - npm run typecheck
    - Failed: package.json has no typecheck script.
    - Re-run at final verification: still missing script.
    - Added script and re-run after timeline helper tests: passed.
    - Re-run after lint configuration and JSX text fixes: passed.
  - npm test -- --watch=false
    - Failed: package.json has no test script.
    - Re-run at final verification: still missing script.
    - Re-run after adding Vitest/tests: passed, 1 test file and 8 tests.
    - Re-run after lint configuration and JSX text fixes: passed, 1 test file and 8 tests.
  - npm test
    - Added Vitest and timeline helper coverage.
    - Passed: 1 test file, 8 tests.
  - npm test -- src/lib/timeline.test.ts
    - Red run failed on malformed date handling, plural injury search, and clinical chip coverage.
    - Green run passed after helper fixes: 8 tests passed.
  - npx tsc --noEmit --pretty false
    - Passed after tsconfig.json was scoped to the Expo app and excluded worker/ and supabase/functions/.
    - Re-run after 7bc02b0: passed.
    - Re-run at final verification: passed.
  - npx tsc --noEmit --pretty false 2>&1 | rg '^src/' || true
    - Passed: no src/ app TypeScript errors reported.
  - npm run build in worker/
    - Passed.
    - Re-run after 7bc02b0: passed.
    - Re-run at final verification: passed.
    - Re-run after lint configuration and JSX text fixes: passed.
  - npx expo-doctor
    - 16/17 checks passed before dependency cleanup; prior remaining failure was duplicate @expo/fingerprint via react-native-health nested @expo/config-plugins.
    - Re-run after @expo/config-plugins override: 17/17 checks passed.
    - Re-run at final verification: 17/17 checks passed.
    - Re-run after adding Vitest/tests: 17/17 checks passed.
    - Re-run after lint configuration and JSX text fixes: 17/17 checks passed.
  - npm audit fix
    - Reduced audit findings.
  - npm audit --audit-level=moderate
    - Previously reported 4 high severity vulnerabilities through react-native-health's nested @expo/config-plugins / @expo/plist / @xmldom/xmldom chain.
    - Re-run after @expo/config-plugins override: found 0 vulnerabilities.
    - Re-run at final verification: found 0 vulnerabilities.
    - Re-run after adding Vitest/tests: found 0 vulnerabilities.
    - Re-run after lint configuration and JSX text fixes: found 0 vulnerabilities.
  - npm ls react-native-health @expo/fingerprint @expo/config-plugins @expo/plist @xmldom/xmldom --all
    - Passed after @expo/config-plugins override; react-native-health now dedupes to @expo/config-plugins@54.0.4 and no longer installs nested @expo/fingerprint or vulnerable @xmldom.
  - npx expo config --type introspect --json
    - Passed after @expo/config-plugins override; Expo evaluated the config including react-native-health plugin.
  - git diff --check
    - Re-run after voice-search changes: passed.
    - Re-run after dependency cleanup: passed.
    - Re-run at final verification: passed.
    - Re-run after adding Vitest/tests: passed.
  - git ls-files AuthKey_ST6W9U8FZ5.p8 .env .env.local
    - Passed: no output; local key/env files are ignored and not tracked.
  - npm run web
    - Passed: Expo web bundle compiled.
    - Re-run after 7bc02b0: passed; Metro served the web app.
    - Re-run after dependency cleanup: passed; Metro served the web app.
  - curl -I http://localhost:8081
    - Passed while dev server was running: HTTP/1.1 200 OK.
    - Re-run after 7bc02b0: passed; HTTP/1.1 200 OK.
    - Re-run after dependency cleanup: passed; HTTP/1.1 200 OK.
  - curl -s http://localhost:8081 | head -40
    - Passed: returned Expo root HTML with title RIVR-Health-AI and root app container.
    - Re-run after dependency cleanup: returned Expo root HTML with title RIVR-Health-AI and root app container.

Manual QA attempt:
- Web app was started with npm run web and served successfully on http://localhost:8081.
- Chrome DevTools MCP could not attach because its browser profile was already locked by an existing Chrome process.
- The repo has no Playwright, Puppeteer, Detox, Jest, or Vitest dependency configured for browser/device flow automation.
- Authenticated end-to-end QA was not performed to avoid creating or mutating data against the configured Supabase environment without test credentials and seeded test records.

7. Code quality requirements
- Evidence:
  - Shared timeline/date/search/clinical helpers live in src/lib/timeline.ts.
  - src/lib/timeline.test.ts covers legacy row normalization, malformed date rejection, age/date display rules, clinical label/tag formatting, natural timeline search, health-card search, and impossible-age fallbacks.
  - No secrets were added.
  - Local AuthKey_ST6W9U8FZ5.p8, .env, and .env.local files are ignored by .gitignore and not tracked by git.
  - No destructive database migrations were added.
  - Existing Supabase query paths remain in place.
  - Changes are scoped to timeline/search/release-health files.
- Status:
  - Implemented for touched areas.

Open risks / deferred items:
- Full manual QA remains required on iOS simulator or a real mobile device using a test account with old and new timeline records.
- Native voice dictation remains deferred by design for release safety; the implemented feature flag enables only the low-risk web speech path where available.
- Root npx tsc --noEmit now validates the Expo app. Supabase Edge Functions are still not typechecked by this command because they use Deno npm:/jsr: imports; worker code is validated separately with worker/npm run build.

Recommended remaining manual QA:
- Login, sign up, forgot password, update password.
- Home loading/empty/error states.
- Manage Documents upload/list/delete/process flows.
- Timeline list on mobile, including old rows with missing title/category/source/summary/data/tags/date_precision.
- Timeline edit/save repeated 5+ times on mobile.
- Toggle Pre-Visit repeatedly from list and details.
- Timeline records older than 24 months, newer than 24 months, missing incident date, missing reported date, and missing patient DOB.
- Search prompts:
  - Show me all injuries from 2018.
  - Find my left thumb injury.
  - What medications was I taking after knee surgery?
  - Show my timeline for shoulder pain.
- Pre-Visit Note generation.
- Share package creation, QR/copy/share modal, and share link open.
- Health Summary / AI Summary rendering.
- Dark and light theme scan on mobile.
